
Parser class which will have some code similar to
	
	switch(token)
		render:
			render_builder -> creat the render object
			
		cons
			cons_builder -> create the cons object
			



Obejct_Builder (or TOkenBuilder) abstract class which will have some empty methods 
which will be overrriden by the concrete classes

Render
	creatREndeer object
	
Cons
	creatCons object 

